services:
  - type: web
    name: medibot
    env: python
    plan: free

    buildCommand: |
      pip install --upgrade pip && pip install -r requirements.txt && python -c "import nltk; nltk.download('punkt_tab'); nltk.download('punkt'); nltk.download('wordnet')"

    startCommand: gunicorn main:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120

    healthCheckPath: /health

    envVars:
      - key: MEDIBOT_SECRET_KEY
        generateValue: true
      - key: FLASK_DEBUG
        value: false
      - key: NLTK_DATA
        value: /tmp/nltk_data
      - key: MEDIBOT_FORCE_TFIDF
        value: true